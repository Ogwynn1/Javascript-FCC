/*
function convertCtoF(celsius) {
  let fahrenheit;
  return fahrenheit;
}

convertCtoF(30);
*/

function convertCtoF(celsius) {
    let fahrenheit = (celsius * 9 / 5 + 32); // Included the C to F formula (C times 9 divided by 5 add 32 = F)
    return fahrenheit;
  }
  
  convertCtoF(30);